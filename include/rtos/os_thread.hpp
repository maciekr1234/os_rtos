#pragma once

#include <rtos/thread/thread.hpp>


// using os_priority     = ::os::rtos::thread::priority;
// using os_thread_state = ::os::rtos::thread::state;
// using os_thread_id    = ::os::rtos::thread::id;
